# Docs Drift Example

This example intentionally introduces a mismatch between the code and the docs.

- Code: `src/users.js`
- Docs: `docs/api.md`

The docs still mention `username`, while the code uses `email`.
